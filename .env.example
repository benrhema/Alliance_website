# Copy to .env for local overrides (optional). On Render, set these in the dashboard.
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
# DATABASE_URL=postgres://...  # Render sets this when you attach PostgreSQL
